#!/bin/bash

if [[ "$1" = "configure" || $1 -ge 1 ]] ; then
  if status "<%= name %>" 2>/dev/null | grep -q ' start/'; then
    # Service is running.
    # It has to be stop+start because upstart doesn't pickup changes with restart.
    stop "<%= name %>"
    start "<%= name %>"
  fi
fi

